import { PlaceholderText } from ".";

export default {
  title: "Components/PlaceholderText",
  component: PlaceholderText,
};

export const Default = {
  args: {
    className: {},
    divClassName: {},
  },
};
